import math as mth;

var PI = 3.14159;

function calculateArea(radius) {
    return mth.multiply(PI, mth.multiply(radius, radius));
}

function calculateCircumference(radius) {
    return mth.multiply(2, mth.multiply(PI, radius));
}